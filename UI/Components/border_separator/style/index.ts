import { StyleSheet } from 'react-native';
import Colors from '@colors';

export default StyleSheet.create({
  BorderSeparator: {
    borderStyle: 'solid',
    alignSelf: 'stretch',
    borderRadius: 100,
  },
});
